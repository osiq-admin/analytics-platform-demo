{
  "kpi_id": "score_distribution",
  "name": "Score Distribution",
  "description": "Alert score histogram in 10-point buckets per detection model",
  "category": "score_distribution",
  "sql_template": "SELECT model_id, FLOOR(score / 10) * 10 as score_bucket, COUNT(*) as count FROM alerts GROUP BY model_id, score_bucket ORDER BY model_id, score_bucket",
  "dimensions": [
    {"field": "model_id", "label": "Detection Model"},
    {"field": "score_bucket", "label": "Score Range"}
  ],
  "schedule": "daily",
  "source_tier": "gold",
  "output_format": "json"
}
